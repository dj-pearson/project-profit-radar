## Consolidated Patterns

### Supabase Query Patterns
- Client: `src/integrations/supabase/client.ts` - createClient with env vars
- Types: `src/integrations/supabase/types.ts` (manually maintained, not auto-generated)
- All queries use RLS (Row Level Security) with company_id isolation
- Pattern: `const { data, error } = await supabase.from('table').select('*').eq('company_id', companyId)`
- Mutations wrapped in TanStack Query: `useMutation({ mutationFn: async () => { ... } })`
- Data fetching hooks use `useQuery` with `enabled: !!user` guard

### Component Architecture
- shadcn/ui components in `src/components/ui/` (Radix UI primitives + Tailwind)
- Domain components organized by feature directory (117+ directories)
- Functional components with hooks only (no class components)
- State: React Context for global (auth, theme), TanStack Query for server state
- Styling: Tailwind CSS with `cn()` utility from `src/lib/utils.ts`
- Import alias: `@/` maps to `src/`

### Authentication & Authorization
- AuthContext at `src/contexts/AuthContext.tsx` - provides `useAuth()` hook
- 6 roles: admin, project_manager, field_supervisor, office_staff, accounting, client_portal
- Route guards: `SecureRoute` component with `allowedRoles` prop
- JWT tokens via Supabase Auth
- ProtectedRoute has circuit-breaker logic for redirect loops

### Routing
- React Router DOM 6.26.2 with lazy loading
- `createLazyRoute()` in `src/utils/lazyRoutes.tsx` wraps routes with Suspense + RouteErrorBoundary
- Route files: appRoutes, financialRoutes, projectRoutes, peopleRoutes, operationsRoutes, marketingRoutes
- Admin pages use `DashboardLayout` wrapper
- Nav config: `src/components/navigation/HierarchicalNavigationConfig.ts`

### Testing Patterns
- Unit: Vitest 4.0.8 + Happy DOM + Testing Library
- E2E: Playwright 1.56.1 (Chromium, Firefox, WebKit)
- Test setup: `src/test/setup.ts`
- A11y testing: `src/test/accessibility-utils.ts` with axe-core
- Coverage: V8 provider, 60% thresholds configured but actual coverage <2%
- Mock Supabase client for unit tests (no real API calls)

### Build & Deploy
- Vite 5.4.1 with manual chunking, tree shaking, code splitting
- Build: `npm run build` (includes sitemap gen, 404 copy, SW version update, image optimization)
- Deploy: Cloudflare Pages from `dist/` folder
- TypeScript: `tsconfig.app.json` covers `src/`, `tsconfig.node.json` covers config files
- `package.json` has `"type": "module"` - CommonJS scripts need `.cjs` extension
- Edge functions are Deno (TypeScript) - NOT checked by `tsc --noEmit`

### Known Pitfalls
- Self-hosted Supabase: foundational tables (user_profiles, companies) may not exist
- Migrations must be idempotent: `CREATE TABLE IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`
- RLS policies referencing `get_user_role()` fail if function doesn't exist - use `auth.uid() IS NOT NULL`
- Env vars must be ALL_CAPS_SNAKE_CASE (previously had mixed-case bugs)
- ~65 unique env vars needed across edge functions
- tsconfig.json extends `expo/tsconfig.base` - root config has Expo integration
- TOAST_LIMIT was 1 (caused dropped notifications) - now set to 5

---

## 2026-02-21 - Session: PRD Stories Batch

### US-001: TypeScript strict mode
- Already enabled: `strict: true`, `noImplicitAny: true`, `strictNullChecks: true` in tsconfig.app.json
- Count script at `scripts/count-strict-violations.sh` existed

### US-002: Remove hardcoded tokens
- Removed hardcoded JWT from `src/lib/envValidation.ts` FALLBACK_VALUES
- Removed PRODUCTION_ANON_KEY from `src/integrations/supabase/client.ts`
- Added fail-fast: console.error in prod, console.warn in dev when key missing

### US-003: Remove debug console statements
- Replaced 41 bare console.* calls across 10 files with `logger` utility
- Files: Setup.tsx, Dashboard.tsx, SecurityService.ts, WorkflowAutomationService.ts, googleAnalyticsSync.ts, AIModelManager.tsx, useGPSLocation.ts, useOfflineSync.ts, useDashboardData.tsx, Invoices.tsx
- Left `supabase/client.ts` console calls alone (circular dep with logger)

### US-004: GitHub Actions CI
- Created `.github/workflows/ci.yml` with 5 jobs: lint, typecheck, unit-test, build, e2e-test
- Node 18, npm caching, Chromium-only E2E

### US-005: Security tests
- Created `src/lib/__tests__/sanitize.test.ts` (30+ tests)
- Created `src/lib/__tests__/envValidation.test.ts` (7 tests)
- Covers sanitizeHtml, stripHtml, encodeUrl, sanitizeInput, sanitizeEmail, sanitizePhone, maskSensitiveData, sanitizeSqlInput

### US-007: Financial calculation unit tests
- Created `src/utils/__tests__/financialCalculations.test.ts` (49 tests)
- Covers all exported functions: calculateProjectCosts, calculateVariance, calculateProfitMargin, calculateMarkup, calculateSellingPrice*, calculateBreakEven, projectCashFlow, calculateCostPerSquareFoot, calculateEarnedValue

### US-008: E2E smoke tests
- Created `tests/e2e/landing.spec.ts` (6 tests: title, elements, SEO, CSS, console errors, TTI)
- Created `tests/e2e/navigation.spec.ts` (8 tests: nav, features, pricing, redirects, 404, auth fields, keyboard)

### US-018: Fix placeholder calculations and mock data
- EnhancedProjectOverview.tsx: Added real Supabase queries for invoices, payments, time_entries
- useDashboardData.tsx: Replaced Math.random() with deterministic calculations
- CRMDashboard.tsx: Wired placeholder handlers to navigation/toast
- bulk-actions.tsx: Added logger.warn for default actions

### US-029: AGENTS.md files
- Created 7 files: src/, src/components/, src/hooks/, src/lib/, src/pages/, supabase/functions/, tests/
- Each documents purpose, conventions, key files, and pitfalls

### US-030: Progress tracking
- Updated this file with all learnings from session

### US-033: Loading states in financial pages
- FinancialDashboard.tsx: Added FinancialDashboardSkeleton component
- Invoices.tsx: Added full-page TableSkeleton before data loads

### US-034: Surface data fetch errors in Dashboard
- Dashboard.tsx: Added Alert + Retry button when useDashboardData returns error
- useDashboardData.tsx: Replaced console.error with logger.error

### US-035: Setup page improvement
- Added supabase.auth.refreshSession() before company creation
- Removed all DEBUG console.log statements

### US-036: Simplify ProtectedRoute
- Rewrote from 213 to ~130 lines
- Replaced dual counter mechanism with single time-windowed trackRedirect()
- Added RecoveryUI with "Re-authenticate" link
- Removed all console.error, replaced with logger

### Learnings
- **DOMPurify in Happy DOM**: `stripHtml` with empty ALLOWED_TAGS may not strip all tags. Test script removal specifically.
- **Puppeteer install**: Use `PUPPETEER_SKIP_DOWNLOAD=true npm install` to avoid CI slowdowns.
- **Build artifacts**: `public/robots.txt` and `public/sitemap.xml` are generated by build scripts — commit them or gitignore them.
- **Supabase client logging**: Can't use `logger` in `supabase/client.ts` due to potential circular imports. Guard with `import.meta.env.DEV`.
- **Mock data cleanup**: Replace `Math.random()` with deterministic calculations based on project fields (e.g., `budget * completion_percentage / 100`).

---

## 2026-02-19 - US-031 (Previously Completed)
- RouteErrorBoundary created at `src/components/RouteErrorBoundary.tsx`
- `createLazyRoute` in `src/utils/lazyRoutes.tsx` wraps lazy components with Suspense + RouteErrorBoundary
- All 7 route files converted from raw `lazy()` to `createLazyRoute`
- Chunk load failures show user-friendly UI with Reload/Dashboard buttons

---

## 2026-02-19 - US-032 (Previously Completed)
- TOAST_LIMIT increased from 1 to 5 in `src/hooks/use-toast.ts`
- Added gap-2 to ToastViewport in `src/components/ui/toast.tsx`
- Multiple toasts now stack correctly without overlapping

---

## 2026-02-23 - US-012: Migrate data tables to AccessibleTable on key pages

### What was done
Migrated 14 pages/components from various table implementations (shadcn Table, raw HTML tables, card grids, div lists) to use the AccessibleTable component for WCAG 2.1 AA compliance.

### Files modified (14 pages)
1. `src/components/estimates/EstimatesTable.tsx` - Replaced shadcn Table with AccessibleTable<Estimate>
2. `src/components/expenses/ExpenseTracker.tsx` - Replaced HTML <table> with AccessibleTable<Expense>
3. `src/components/invoices/InvoiceList.tsx` - Replaced card layout with AccessibleTable
4. `src/pages/TeamManagement.tsx` - Replaced card list with AccessibleTable<TeamMember>
5. `src/pages/ChangeOrders.tsx` - Replaced card layout with AccessibleTable<ChangeOrder>
6. `src/pages/DailyReports.tsx` - Replaced card grid with AccessibleTable
7. `src/pages/Equipment.tsx` - Replaced card grid with AccessibleTable
8. `src/pages/CRMContacts.tsx` - Replaced ResponsiveGrid with AccessibleTable
9. `src/pages/admin/ErrorLogs.tsx` - Replaced collapsible list with AccessibleTable
10. `src/pages/admin/Users.tsx` - Replaced list items with AccessibleTable
11. `src/pages/admin/PermissionManagement.tsx` - Replaced card lists with AccessibleTable (two tables: Permissions + Roles)
12. `src/pages/AuditLogs.tsx` - Added AccessibleTable with AuditLogEntry interface
13. `src/pages/DocumentManagement.tsx` - Replaced DocumentCard grid with AccessibleTable
14. `src/components/time-tracking/TimeEntriesList.tsx` - Replaced div list with AccessibleTable

### Key patterns used
- `TableColumn<T>` interface with `key`, `header`, `sortable`, `render`, `hideOnMobile`, `align`, `headerRender`
- Actions columns use `headerRender: () => <span className="sr-only">Actions</span>` for screen reader only header
- Rich content preserved via `render` functions (badges, avatars, dropdown menus, icons)
- All icon-only buttons have `aria-label` attributes
- Decorative icons have `aria-hidden="true"`
- Caption provided via `caption` prop (with `hideCaption` for visual hiding when page heading serves as caption)

### Accessibility features gained
- Proper `<th>` scope attributes on all headers
- Keyboard navigation between cells (arrow keys)
- Sortable column announcements to screen readers
- Proper table semantics with `<caption>`, `<thead>`, `<tbody>`
- Screen reader announcements for sort state changes

### Learnings
- Card-to-table conversion: Use AccessibleTable's `render` function to maintain rich content (badges, icons, avatars, action menus) within table cells
- Orphaned code cleanup: When replacing `.map()` loops with AccessibleTable, ensure all leftover JSX from the loop body is removed (variables from the loop iterator go out of scope)
- The AccessibleTable component requires `T extends { id: string | number }` - data objects need an `id` field

---

## Session: US-009 - Fix critical any types in core modules
**Date**: 2026-02-23
**Branch**: enterprise-production-readiness (merged from main)
**Status**: COMPLETE

### Summary
Removed `any` types and `@ts-expect-error` comments from core modules:
- **src/contexts/**: Already clean - no `any` types found
- **src/lib/**: Fixed 25+ `any` types across 8 files (analytics.ts, cache.ts, offline-sync.ts, queryClient.ts, touchOptimization.ts, sessionFingerprint.ts, react-native-web-fallback.ts, mobile-touch.ts)
- **src/hooks/**: Fixed 22 hooks (exceeds 20 requirement)

### Hooks fixed (22 total)
useRealtimeSubscription, useRealTimeSync, useSupabaseMutation, useSupabaseQuery, useSecurity, useNotifications, useAuditLog, useTasks, usePerformanceMonitor, useLongPress, useTouchGestures, useTouchDevice, useInstallPrompt, usePWA, useOfflineSync, useSimpleNotifications, useAccessibilityHelpers, useDeviceTrust, useAccounting, useAutoClockInOut, useActivityTracking, useAutomatedSocialPosts

### Patterns used
- `fromTable()` helper for dynamic Supabase table access: `supabase.from(table as keyof Database['public']['Tables'])`
- `ReturnType<typeof setTimeout>` instead of `NodeJS.Timeout` for timer refs
- Intersection types for non-standard browser APIs: `Navigator & { deviceMemory?: number }`
- `'prop' in navigator` guard before casting for non-standard props (msMaxTouchPoints, standalone, deviceMemory)
- `catch (error: unknown)` + `error instanceof Error ? error.message : String(error)` pattern
- `Record<string, unknown>` replacing `Record<string, any>` throughout
- `EventListener` cast for addEventListener callbacks instead of `as any`

### Quality checks
- `npx tsc --noEmit`: PASS (zero errors)
- `npm run build`: PASS (built in 3m 17s)
- No runtime behavior changes

---

## 2026-02-23 - US-013: Migrate dialogs and modals to AccessibleModal

### What was done
Migrated 20 critical Dialog/AlertDialog components to AccessibleModal for WCAG 2.1 AA compliance with focus trapping, escape key handling, and screen reader support.

### Files modified (20 modals)
1. `src/components/tasks/CreateTaskDialog.tsx` - Create task form
2. `src/components/tasks/EditTaskDialog.tsx` - Edit task form
3. `src/components/estimates/ConvertToProjectDialog.tsx` - Estimate→project wizard
4. `src/components/mfa/MFAVerificationModal.tsx` - MFA 6-digit code verification
5. `src/components/auth/MFASetupDialog.tsx` - MFA setup wizard (3 steps)
6. `src/pages/Projects.tsx` - Edit project + delete confirmation
7. `src/pages/CRMContacts.tsx` - New contact form
8. `src/pages/admin/Users.tsx` - User detail + impersonation confirmation
9. `src/components/estimates/EstimatesTable.tsx` - Edit estimate + delete confirmation
10. `src/components/expenses/ExpenseTracker.tsx` - Create/edit expense + delete confirmation
11. `src/pages/EstimatesHub.tsx` - Create estimate dialog
12. `src/components/filters/FilterPresetsManager.tsx` - Save preset + delete confirmation
13. `src/components/crm/EmailSyncSetup.tsx` - Delete email account confirmation
14. `src/components/bonds/BondForm.tsx` - Add/edit bond form
15. `src/components/permits/PermitForm.tsx` - Add/edit permit form
16. `src/components/documents/DocumentCard.tsx` - Document versions viewer
17. `src/components/job-costing/BudgetManager.tsx` - Budget item form + delete confirmation
18. `src/pages/admin/SSOManagement.tsx` - SSO config + MFA setup + delete confirmation
19. `src/components/payments/PaymentMethodManager.tsx` - Add payment method
20. `src/components/workflow/ChangeOrderManagement.tsx` - Create/edit change order

### Key patterns used
- `title` prop is string-only - stripped JSX icons from DialogTitle text
- Size mapping: max-w-md→sm, max-w-lg→md, max-w-2xl→lg, max-w-4xl→xl
- AlertDialog→AccessibleModal with `disableClickOutside` + destructive footer buttons
- DialogTrigger removed - replaced with state-controlled button + `isOpen` state
- Forms with footer buttons: used `id="form-name"` on form + `form="form-name"` on submit button in footer
- MFA modals: `disableClickOutside={true}`, `disableEscapeKey={true}` for security-critical flows
- MFASetupDialog: conditional close prevention with `hideCloseButton`/`disableClickOutside`/`disableEscapeKey` when MFA is required

### Accessibility features gained
- Focus trap in all modals (Tab cycles within modal)
- Escape key closes modal (except security-critical flows)
- Focus returns to trigger element when modal closes
- All modals have `aria-labelledby` and `aria-describedby` attributes
- `aria-modal="true"` and `role="dialog"` on all modals
- Body scroll lock when modal is open

### Learnings
- DialogTrigger pattern (uncontrolled): Must add state variable (`showDialog`) and separate button onClick from dialog open/close
- Forms in footer: Use HTML `form` attribute to connect external submit button to form element
- Security modals: Use `disableClickOutside` + `disableEscapeKey` + `hideCloseButton` to prevent accidental dismissal
- MFASetupDialog has 3 separate dialog renders for steps (intro/setup/success) - each needs independent AccessibleModal

---
