# Handle auth recovery redirects properly
/auth?error_recovery*  /auth  302

# SPA routing is handled by 404.html (automatically served by Cloudflare Pages)
# See scripts/copy-404.js which creates 404.html from index.html during build
