# Optimize for faster builds
progress=false
audit=false
fund=false
save-exact=true

# Use faster registry
registry=https://registry.npmjs.org/

# Enable package-lock optimizations
package-lock-only=false
prefer-offline=true

# Reduce network requests
maxsockets=10

# Allow optional dependencies (needed for Rollup native binaries)
